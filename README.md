# syndacate-public

Public code implementation for SynDaCaTE paper.

## Installation

```
python -m pip install -U pip
python -m pip install -U jutility==0.0.24
python -m pip install -U juml-toolkit==0.0.3
python -m pip install git+https://github.com/jakelevi1996/syndacate-public.git
```
